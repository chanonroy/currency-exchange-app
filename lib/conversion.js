/**
 * Represents a book.
 * @param {number} fromAmount - Amount of currency to convert from.
 * @param {float} conversionRate - The conversion rate.
 * @return {number} convertedCurrency - Amount of currency to convert to.
 */
const convertCurrency = ({ amount, conversionRate }) => {
  
  return null;
};

export default convertCurrency;
